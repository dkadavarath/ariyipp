<?xml version="1.0" encoding="utf-8"?>
<!-- The ariyipp wordmark, tinted to the themed ink colour (dark on light, light on dark). The
     bitmap alpha is the glyph mask; padding for the adaptive safe zone is baked into the bitmap. -->
<bitmap xmlns:android="http://schemas.android.com/apk/res/android"
    android:src="@mipmap/ic_launcher_fg"
    android:gravity="fill"
    android:tint="@color/ic_launcher_ink" />
