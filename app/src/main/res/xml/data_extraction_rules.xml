<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ (targetSdk 31+) backup/transfer rules. We disallow both cloud backup and
    device-to-device transfer entirely: the local DB holds sensitive notification content and
    the settings store holds the webhook auth token, none of which should leave the device.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
