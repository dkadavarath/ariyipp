<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Companion-role permissions; merged into the app. Requested at runtime only in companion mode. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- No application attributes / launcher / Application class: the app module owns those. The
         companion FCM receiver is intentionally not declared here yet — the merged app uses a single
         FirebaseMessagingService that dispatches by role (wired in the next step). -->
    <application>

        <activity android:name=".ui.PairingActivity" android:exported="false" />
        <activity android:name=".ui.SimNamesActivity" android:exported="false" />
        <activity android:name=".ui.WebhookActivity" android:exported="false" />
        <activity android:name=".ui.AppearanceActivity" android:exported="false" />
        <activity android:name=".ui.DiagnosticsActivity" android:exported="false" />
        <activity android:name=".ui.HelpActivity" android:exported="false" />

        <!-- Observe incoming SMS; only the system (BROADCAST_SMS holder) may trigger it. -->
        <receiver
            android:name=".sms.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- Persistent foreground service so the OS keeps the relay process warm. -->
        <service
            android:name=".KeepAliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps the SMS relay alive so incoming SMS are forwarded reliably." />
        </service>

        <!-- Re-arm keep-alive + relay scan after reboot. -->
        <receiver
            android:name=".boot.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
